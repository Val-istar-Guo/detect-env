import { expect } from 'chai';
import create from '../src/create';


describe('env creator', function () {
  it('can custom alias')

  it('can custom environment variables')

  it('can custom shortcut')
});
